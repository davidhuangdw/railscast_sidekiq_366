= simple_form_for(@snippet) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :language, collection: %w{ruby javascript c++ c}
    = f.input :plain_code, input_html:{rows:"10"}

  .form-actions
    = f.button :submit
